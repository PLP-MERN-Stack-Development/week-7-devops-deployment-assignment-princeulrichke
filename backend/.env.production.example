# Backend Environment Variables for Render
NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chatapp?retryWrites=true&w=majority
JWT_SECRET=your_super_secure_jwt_secret_change_this_in_production_minimum_64_characters
CORS_ORIGIN=https://your-frontend-app.vercel.app
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Instructions:
# 1. Replace MONGODB_URI with your actual MongoDB Atlas connection string
# 2. Generate a strong JWT_SECRET (minimum 64 characters)
# 3. Replace CORS_ORIGIN with your actual Vercel frontend URL after deployment
